name: Test Runner

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: self-hosted

    steps:
      - name: Hello
        run: echo "Runner is working!"

      - name: System Info
        run: |
          hostname
          whoami
          pwd
